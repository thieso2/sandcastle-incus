# Tenant Tailnet per Tenant

Sandcastle assigns each Tenant its own Tenant Tailnet instead of placing all tenant machines in one shared Sandcastle tailnet. This keeps Machine SSH Access, private DNS, and network membership scoped to a single Tenant, accepting extra provisioning and operational complexity for stronger tenant isolation.
